// const multer = require('multer');

// // Set up the storage configuration
// const storage = multer.memoryStorage(); // This stores the uploaded file as a buffer in memory
// const upload = multer({ storage: storage });